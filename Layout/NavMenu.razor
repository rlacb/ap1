<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">DPHSCAN</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable">
    <nav class="flex-column nav-container">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="weather">
                Philippine Major Attractions
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="commu">
                Effective Communication
            </NavLink>
        </div>
        <br
        <div class="nav-item">
            <NavLink class="nav-link" href="roles">
                Roles and Responsibilities of a Tour Guide
            </NavLink>
        </div>
    </nav>
</div>


<script>
    document.addEventListener('DOMContentLoaded', function () {
        const navLinks = document.querySelectorAll('.nav-link');
        const cameraEntity = document.getElementById('cameraEntity');

        navLinks.forEach(link => {
            link.addEventListener('click', function () {
                if (cameraEntity) {
                    cameraEntity.setAttribute('visible', 'false');
                }
            });
        });
    });
</script>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
